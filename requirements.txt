# =============================================================================
# Quantitative Trading Platform — Python Dependencies
# =============================================================================

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
python-multipart>=0.0.9      # form data (OAuth2 login form)

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0               # database migrations
psycopg2-binary>=2.9.9        # PostgreSQL driver (Railway)

# Authentication
python-jose[cryptography]>=3.3.0   # JWT encode/decode
passlib[bcrypt]>=1.7.4             # password hashing

# Rate limiting
slowapi>=0.1.9

# Caching
redis>=5.0.0                  # Redis client (optional — app degrades gracefully)

# Task queue (optional — app falls back to threading without CELERY_BROKER_URL)
celery>=5.3.0

# Quant / Data Science
polars>=0.20.0
numpy>=1.26.0
scipy>=1.12.0
statsmodels>=0.14.0
scikit-learn>=1.4.0
pandas>=2.0.0

# Market data
yfinance>=0.2.36

# HTTP clients
httpx>=0.27.0
websockets>=12.0

# Error tracking (optional — configure SENTRY_DSN env var)
sentry-sdk[fastapi]>=1.45.0
