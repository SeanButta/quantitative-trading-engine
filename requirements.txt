# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.110.0
python-jose[cryptography]>=3.3.0   # JWT signing/verification (auth.py)
passlib[bcrypt]>=1.7.4             # password hashing (auth.py)
uvicorn[standard]>=0.29.0      # ASGI server with websocket support
pydantic>=2.0.0
python-multipart>=0.0.9        # form/file uploads

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9         # PostgreSQL adapter (Supabase / any PG instance)

# ── Data science ──────────────────────────────────────────────────────────────
polars>=0.20.0                 # fast DataFrames (feature engine, signal engine)
numpy>=1.26.0
scipy>=1.12.0
statsmodels>=0.14.0
scikit-learn>=1.4.0
pandas>=2.0.0

# ── Market data ───────────────────────────────────────────────────────────────
yfinance>=0.2.36               # primary price / options data source

# ── HTTP / async ──────────────────────────────────────────────────────────────
websockets>=12.0               # WebSocket endpoint
httpx>=0.27.0                  # async HTTP (used by some internal calls)
requests>=2.31.0               # sync HTTP fallback (sec_feed, fred_data)
