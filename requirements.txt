# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.0
python-jose[cryptography]==3.5.0   # JWT signing/verification (auth.py)
passlib[bcrypt]>=1.7.4             # kept for any legacy verify paths; bcrypt direct-use
bcrypt==5.0.0                      # password hashing — used directly in auth.py
uvicorn[standard]==0.30.6          # ASGI server with websocket support
gunicorn>=21.2.0                   # multi-worker process manager (4 workers in prod)
slowapi==0.1.9                     # per-IP rate limiting for FastAPI
pydantic==2.9.2
python-multipart>=0.0.9            # form/file uploads

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.35
psycopg2-binary>=2.9.9             # PostgreSQL adapter (Supabase / any PG instance)

# ── Data science ──────────────────────────────────────────────────────────────
polars==1.39.3                     # fast DataFrames (feature engine, signal engine)
numpy==2.4.2
scipy==1.17.0
statsmodels>=0.14.0
scikit-learn==1.8.0
pandas>=2.0.0

# ── Market data ───────────────────────────────────────────────────────────────
yfinance==1.2.0                    # free data source (default, rate-limited)
polygon-api-client==1.16.3         # paid data source ($29-199/mo, no rate limits)
anthropic==0.86.0                  # Claude AI synthesis (optional)

# ── HTTP / async ──────────────────────────────────────────────────────────────
websockets>=12.0                   # WebSocket endpoint
httpx==0.27.2                      # async HTTP (used by some internal calls)
requests==2.32.5                   # sync HTTP fallback (sec_feed, fred_data)
